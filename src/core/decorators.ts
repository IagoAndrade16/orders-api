/**
 * Decorator criado para sinalizar que uma função não é usada fora daquela classe,
 * mas é necessária nos arquivos de teste
 * */
export const visibleForTesting = (target: any, key: string) => {
	//
};
